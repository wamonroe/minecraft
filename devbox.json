{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.13.7/.schema/devbox.schema.json",
  "packages": {
    "git":    "latest",
    "ruby":   "latest",
    "libyaml": {
      "version": "latest",
      "outputs": ["out", "dev"]
    }
  },
  "shell": {
    "init_hook": [],
    "scripts": {
      "console": [
        "ruby lib/console.rb"
      ],
      "restart-proxy": [
        "docker compose restart mc_router"
      ],
      "setup": [
        "bundle install"
      ],
      "update-modpack": [
        "git checkout -- devbox.lock",
        "git pull --ff-only",
        "ruby lib/update_modpack.rb"
      ],
      "update-server": [
        "OLD_HEAD=$(git rev-parse HEAD)",
        "git pull --ff-only",
        "git diff --name-only $OLD_HEAD HEAD | grep -q 'docker-compose.yml' && docker compose down && docker compose up -d || true"
      ]
    }
  }
}
